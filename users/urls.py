from django.conf.urls import url

urlpatterns = [
    # url(r'^signup/$', UsersSignup.as_view(), name='signup'),
    # url(r'^login/$', UsersLogin.as_view(), name='signup'),
    # url(r'^(?P<pk>\d+)/$', UsersProfile.as_view(), name='profile'),
    # url(r'^(?P<pk>\d+)/edit/$', UsersEdit.as_view(), name='edit'),
    # url(r'^(?P<pk>\d+)/delete/$', UsersDelete.as_view(), name='delete'),
]
